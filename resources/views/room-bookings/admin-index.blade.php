@extends('layouts.app')
@section('content')
<div class="mb-4"><h2>Room Bookings</h2><p class="text-muted">Review reservations and update their status.</p></div>
<div class="card"><div class="table-responsive"><table class="table table-dark table-hover mb-0" style="--bs-table-bg: transparent; --bs-table-border-color: var(--border-color);"><thead><tr><th class="ps-4">Room</th><th>Member</th><th>Schedule</th><th>Purpose</th><th>Status</th><th class="text-end pe-4">Update</th></tr></thead><tbody>@forelse($bookings as $booking)<tr><td class="ps-4">{{ $booking->room->name }}</td><td>{{ $booking->user->name }}</td><td>{{ $booking->start_at->format('d M Y H:i') }}<br><span class="text-muted">to {{ $booking->end_at->format('d M Y H:i') }}</span></td><td>{{ $booking->purpose }}</td><td><span class="badge {{ $booking->status === 'approved' ? 'bg-success' : ($booking->status === 'cancelled' ? 'bg-danger' : 'bg-warning text-dark') }}">{{ ucfirst($booking->status) }}</span></td><td class="text-end pe-4"><form method="POST" action="{{ route('admin.room-bookings.status', $booking) }}" class="d-flex gap-2 justify-content-end">@csrf @method('PATCH')<select name="status" class="form-select form-select-sm" style="width: 130px"><option value="pending" @selected($booking->status === 'pending')>Pending</option><option value="approved" @selected($booking->status === 'approved')>Approved</option><option value="completed" @selected($booking->status === 'completed')>Completed</option><option value="cancelled" @selected($booking->status === 'cancelled')>Cancelled</option></select><button class="btn btn-sm btn-outline-secondary">Save</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No room bookings yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $bookings->links() }}</div>
@endsection
