@extends('layouts.app')
@section('content')
<div class="mb-4"><h2>My Room Bookings</h2><p class="text-muted">Track and cancel your upcoming faculty room reservations.</p></div>
<div class="card"><div class="table-responsive"><table class="table table-dark table-hover mb-0" style="--bs-table-bg: transparent; --bs-table-border-color: var(--border-color);"><thead><tr><th class="ps-4">Room</th><th>Schedule</th><th>Purpose</th><th>Status</th><th class="text-end pe-4">Action</th></tr></thead><tbody>@forelse($bookings as $booking)<tr><td class="ps-4"><strong>{{ $booking->room->name }}</strong><div class="text-muted small">{{ $booking->room->location }}</div></td><td>{{ $booking->start_at->format('d M Y H:i') }}<br><span class="text-muted">to {{ $booking->end_at->format('d M Y H:i') }}</span></td><td>{{ $booking->purpose }}</td><td><span class="badge {{ $booking->status === 'approved' ? 'bg-success' : ($booking->status === 'cancelled' ? 'bg-danger' : 'bg-warning text-dark') }}">{{ ucfirst($booking->status) }}</span></td><td class="text-end pe-4">@if($booking->status === 'approved' && $booking->start_at->isFuture())<form method="POST" action="{{ route('room-bookings.cancel', $booking) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-danger">Cancel</button></form>@else<span class="text-muted">-</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">You have no room bookings.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $bookings->links() }}</div>
@endsection
