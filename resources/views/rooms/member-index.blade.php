@extends('layouts.app')
@section('content')
<div class="mb-4"><h2>Book a Faculty Room</h2><p class="text-muted">Choose an available room and submit your reservation.</p></div>
<div class="row g-4">@forelse($rooms as $room)<div class="col-md-6 col-xl-4"><div class="card h-100"><div class="card-body"><div class="d-flex justify-content-between"><h5>{{ $room->name }}</h5><span class="badge bg-success">Available</span></div><p class="text-muted small mb-1"><i class="bi bi-geo-alt"></i> {{ $room->location ?: 'Location not specified' }}</p><p class="text-muted small">Capacity: {{ $room->capacity }} people</p><p>{{ $room->description ?: 'Faculty room available for academic activities.' }}</p><form method="POST" action="{{ route('rooms.book', $room) }}">@csrf<div class="mb-2"><label class="form-label">Start</label><input type="datetime-local" name="start_at" class="form-control" min="{{ now()->format('Y-m-d\TH:i') }}" required></div><div class="mb-2"><label class="form-label">End</label><input type="datetime-local" name="end_at" class="form-control" required></div><div class="mb-2"><label class="form-label">Purpose</label><input name="purpose" class="form-control" maxlength="255" required placeholder="e.g. Faculty meeting"></div><div class="mb-3"><label class="form-label">Notes</label><textarea name="notes" class="form-control" rows="2"></textarea></div><button class="btn btn-primary w-100"><i class="bi bi-calendar-plus"></i> Book Room</button></form></div></div></div>@empty<div class="col-12"><div class="card"><div class="card-body text-center text-muted py-5">No rooms are currently available.</div></div></div>@endforelse</div>
@endsection
